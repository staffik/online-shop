<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1" charset="UTF-8">
    <link rel="stylesheet" href="/w3.css">
    <title>Item</title>
</head>
<body>
    <% include navbar %>
    <div class="w3-container">
    <div class="w3-row-padding">
        <div class="w3-col s6 w3-center">
            <div>
            <img src="<%= item.img %>" style="width:100%; height:300px;">
            </div>
            <div class="w3-btn-group">
                <% if(user == "admin") { %>
                    <a href="/modify_item?id=<%= item._id %>&name=<%= item.name %>" class="w3-btn w3-green" style="width:50%"> Modify </a>
                    <a href="/delete_item?id=<%= item._id %>" class="w3-btn w3-green" style="width:50%"> Delete </a>
                <% } else if(user) { %>
                    <a href="/buy_item?id=<%= item._id %>" class="w3-btn w3-green" style="width:100%">Buy</a>
                <% } %>
            </div>
        </div>
        <div class="w3-col s6 w3-center">
            <div class="w3-panel w3-grey">
                <p><h3><%= item.name %></h3></p>
            </div>
            <div class="w3-panel w3-grey">
                <label class="w3-text-black">Price</label>
                <p><%= item.price %> $</p>
            </div>
            <div class="w3-panel w3-grey">
                <label class="w3-text-black">Description</label>
                <p><%= item.desc %></p>
            </div>
        </div>
    </div>
    </div>
</body>
</html>